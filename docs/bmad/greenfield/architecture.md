# BMAD Greenfield Architecture

## Stack
Vite, React, static data modules, plain CSS, GitHub Pages.

## Data Model
`curriculum → tracks → units → lessons → challenge`.

## State
Local React state for active view, active track, active lesson and session answers.
